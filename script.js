function getComputerChoice() {
    let options = ['Rock', 'Paper', 'Scissors']
    let randomized = console.log(options[(Math.floor(Math.random() * options.length))])
    return randomized
}
getComputerChoice();

function singleRound(playerSelection, computerSelection) {

}